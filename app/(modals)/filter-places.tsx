import { Text, View } from 'react-native';

const FilterPlacesModal = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Filter places</Text>
    </View>
  );
};

export default FilterPlacesModal;
